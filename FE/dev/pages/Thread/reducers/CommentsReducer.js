
const CommentsReducer =( state = null, action) =>{

    switch(action.type) {
        case 'PLACEHOLDER':
            //state = {...};
            break;
    }
    return state;

}

export default CommentsReducer;